export type CompensationTarget = {
    /**
     * The optics ID to set compensation on.
     */
    opticsId: string;
    /**
     * Specifies if active compensation should be enabled or disabled.
     */
    enable: boolean;
};
